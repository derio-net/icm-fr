# Stage NN — <stage job> (Layer 2 contract)

<!--
  The three sections below are required and must keep these exact headers
  (## Inputs / ## Process / ## Outputs) — validate.py checks for them.
  Tag every Inputs line (reference) for Layer 3 or (working) for Layer 4.
  Name no harness and no harness-specific tool here — relative paths only.
-->

## Inputs
- Layer 4 (working): ../<NN-1>_<prev>/output/      # previous stage's output
- Layer 3 (reference): ../../_config/voice.md       # stable rule / style

## Process
1. <what this stage does, step by step>
2. <follow the reference; transform the working input>

## Outputs
- <artifact>.md -> output/
